// FitVids
$(document).ready(function(){
	// Target your .container, .wrapper, .post, etc.
	$("#main").fitVids();
});

$(function(){
	$('.your-class').slick({
	  dots: true,
	  autoplay: true,
	  autoplaySpeed: 4000,
	  easing: 'swing',
	  pauseOnDotsHover: true,
	  fade: true,
	  arrows: true,
  });
});
